# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # AUDITORÍA v1.5.0: Incrementado maxInstances para prevenir DoS y mejorar resiliencia
  maxInstances: 3
  minInstances: 0
